{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for sidebar, topbar, and dialog webviews",
  "webviews": ["sidebar", "topbar", "dialog"],
  "permissions": [
    "core:default",
    "core:event:default",
    "shell:allow-open",
    "dialog:allow-open"
  ]
}
